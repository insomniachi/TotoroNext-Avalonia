<ContentPage xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:TotoroNext.Anime.ViewModels"
             xmlns:u="https://irihi.tech/ursa"
             xmlns:mdl="totoro.next/models"
             xmlns:ctl="totoro.next/controls"
             xmlns:module="clr-namespace:TotoroNext.Module;assembly=TotoroNext.Module"
             xmlns:extensions="totoro.next/extensions"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             Width="650"
             x:DataType="vm:AnimeExtensionsViewModel"
             Background="{DynamicResource SemiColorBackground2}"
             x:Class="TotoroNext.Anime.Views.AnimeExtensionsView">
    <Grid RowDefinitions="*, Auto, Auto">
        <ScrollViewer Grid.Row="0">
            <StackPanel Classes.SettingsContainer="true">
                <ctl:SettingsCard Header="Incognito"
                                  Description="Don't show discord rich presence for this anime"
                                  Icon="{extensions:Geometry Key={x:Static module:CommonIcons.Incognito}}">
                    <ctl:SettingsCard.Editor>
                        <ToggleSwitch IsChecked="{Binding IsNsfw}" />
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Provider"
                                  Description="The provider to use for this anime"
                                  Icon="{extensions:Geometry Key={x:Static module:CommonIcons.VideoLibrary}}">
                    <ctl:SettingsCard.Editor>
                        <ComboBox ItemsSource="{Binding Providers}"
                                  SelectedValue="{Binding ProviderId}"
                                  SelectedValueBinding="{Binding Id}"
                                  DisplayMemberBinding="{Binding Name}"
                                  HorizontalAlignment="Stretch" />
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:DataContainerEditor Options="{Binding ProviderOptions}"
                                         IsVisible="{Binding ProviderOptions.Count}" />
                <ctl:SettingsCard IsVisible="{Binding HasProvider}"
                                  Header="Selected Result">
                    <ctl:SettingsCard.Editor>
                        <u:AutoCompleteBox x:Name="AutoCompleteBox"
                                           Text="{Binding SearchTerm}"
                                           SelectedItem="{Binding ProviderResult}"
                                           ValueMemberBinding="{ReflectionBinding Title}"
                                           FilterMode="None"
                                           MaxDropDownHeight="300"
                                           MinimumPopulateDelay="0:0:0.500"
                                           MinimumPrefixLength="2"
                                           Width="200">
                            <u:AutoCompleteBox.ItemTemplate>
                                <DataTemplate x:DataType="mdl:SearchResult">
                                    <TextBlock Text="{Binding Title}"
                                               ToolTip.Tip="{Binding Title}"
                                               TextTrimming="CharacterEllipsis"
                                               Width="200" />
                                </DataTemplate>
                            </u:AutoCompleteBox.ItemTemplate>
                        </u:AutoCompleteBox>
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Opening Skip Method"
                                  Description="How to skip the opening of this anime"
                                  Icon="{extensions:Geometry Key={x:Static module:CommonIcons.Opening}}">
                    <ctl:SettingsCard.Editor>
                        <u:EnumSelector EnumType="mdl:SkipMethod"
                                        Value="{Binding OpeningSkipMethod, Mode=TwoWay }"
                                        HorizontalAlignment="Stretch" />
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Ending Skip Method"
                                  Description="How to skip the ending of this anime"
                                  Icon="{extensions:Geometry Key={x:Static module:CommonIcons.Ending}}">
                    <ctl:SettingsCard.Editor>
                        <u:EnumSelector EnumType="mdl:SkipMethod"
                                        Value="{Binding EndingSkipMethod, Mode=TwoWay }"
                                        HorizontalAlignment="Stretch" />
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Use absolute numbering"
                                  Description="Uses anime relations to map episodes to seasons">
                    <ctl:SettingsCard.Editor>
                        <ToggleSwitch IsChecked="{Binding UseAbsoluteNumbering}" />
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Create season map"
                                  Description="Create a season map to enable tracking when anime provider uses absolute numbering">
                    <ctl:SettingsCard.Editor>
                        <Button Content="Create"
                                Command="{Binding BuildRelationshipTreeCommand}"/>
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
                <ctl:SettingsCard Header="Delete"
                                  Description="Delete overrides to anime and use defaults">
                    <ctl:SettingsCard.Editor>
                        <Button Content="Delete"
                                Command="{Binding DeleteCommand}"/>
                    </ctl:SettingsCard.Editor>
                </ctl:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentPage>